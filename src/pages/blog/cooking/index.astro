---
import { getCollection } from "astro:content";
import BaseLayout from "../../../layouts/BaseLayout.astro";

const posts = (await getCollection("blogCooking"))
  .filter((post) => !post.data.draft)
  .sort((a, b) => {
    // Newest publish date first.
    const aTime = a.data.pubDate ? new Date(a.data.pubDate).getTime() : -Infinity;
    const bTime = b.data.pubDate ? new Date(b.data.pubDate).getTime() : -Infinity;
    return bTime - aTime;
  });
---

<BaseLayout
  title="Cooking Blog — Alex Herek"
  description="Polish Christmas Eve traditions, regional recipes, and menu-planning guides tied to the Wigilia cookbook."
  current="cooking"
>
  <div class="wrap">
    <div class="crumb"><a href="/">Home</a><span>/</span><a href="/blog">Blog</a><span>/</span>Cooking</div>
  </div>

  <div class="wrap" style="padding-top: 34px;">
    <div class="section-head reveal" style="text-align: left; margin: 0 0 30px;">
      <span class="eyebrow" style="color: var(--cranberry-deep);">COOKING BLOG</span>
      <h1 style="font-family: 'Fraunces', serif; font-size: 2.3rem; color: var(--navy); font-weight: 600;">Traditions, tables &amp; recipes.</h1>
      <p>Polish Christmas Eve customs, regional dishes, and menu-planning guides — tied back to <a href="/cooking/wigilia">Wigilia: A Polish Christmas Cookbook of Three Tables</a>.</p>
    </div>
  </div>

  <section class="wrap" style="padding-top: 0;">
    <div class="post-grid">
      {posts.map((post) => (
        <a href={`/blog/cooking/${post.id}`} class="post-card reveal">
          {post.data.heroImage ? (
            <img class="post-thumb cooking" src={post.data.heroImage} alt={post.data.heroImageAlt ?? ""} />
          ) : (
            <div class="post-thumb cooking"></div>
          )}
          <div class="post-body">
            <span class="post-tag cooking">{post.data.tag}</span>
            <h3>{post.data.title}</h3>
            <p>{post.data.excerpt}</p>
            <span class="post-date">{post.data.pubDate?.toDateString()}</span>
          </div>
        </a>
      ))}
    </div>
  </section>
</BaseLayout>

<style>
  .post-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 26px; }
  @media (max-width: 900px) { .post-grid { grid-template-columns: 1fr 1fr; } }
  @media (max-width: 600px) { .post-grid { grid-template-columns: 1fr; } }
  .post-card { background: var(--paper); border: 1px solid rgba(198,153,47,.28); border-radius: var(--radius-m); overflow: hidden; display: flex; flex-direction: column; text-decoration: none; }
  .post-thumb { aspect-ratio: 16/10; background: linear-gradient(155deg,var(--cream-deep),var(--paper)); width: 100%; object-fit: cover; display: block; }
  .post-thumb.cooking { background: linear-gradient(155deg,var(--cranberry-tint),var(--paper)); }
  .post-body { padding: 20px; display: flex; flex-direction: column; gap: 8px; }
  .post-tag { font-family: 'IBM Plex Mono', monospace; font-size: .62rem; letter-spacing: .06em; text-transform: uppercase; color: var(--coral-deep); }
  .post-tag.cooking { color: var(--cranberry-deep); }
  .post-body h3 { font-size: 1.02rem; color: var(--navy); line-height: 1.3; }
  .post-body p { margin: 0; font-size: .84rem; color: var(--ink-soft); line-height: 1.5; }
  .post-date { font-family: 'IBM Plex Mono', monospace; font-size: .68rem; color: var(--ink-soft); margin-top: 6px; }
</style>
